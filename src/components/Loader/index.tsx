import { JSX } from "react";

export const Loader = (): JSX.Element => (
  <div className="w-12 h-12 border-7 border-blue-200 border-t-blue-500 rounded-full animate-spin absolute bottom-[32px] right-[32px]" />
);
